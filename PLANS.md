<!----------------------------------------------------------------------------->
# TODO
<!----------------------------------------------------------------------------->

- try/catch
    - add builtins
    - add Kontinue handlers in Machine


```
  // On error, format it nicely:
  const badTerm = program[0];
  if (badTerm.loc) {
    console.log(badTerm.loc.source.formatError(badTerm.loc, 'Something went wrong'));
  }
```

















<!----------------------------------------------------------------------------->
